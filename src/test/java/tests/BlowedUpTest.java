package tests;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

/**
 * ez a class azért van dokumentálva, mert az a fájl MÁR létezik :)
 */
public class BlowedUpTest {

    /**
     * ez egy csodálatos teszt, azért, mert a csodálatos intellij nem volt képes sample.BlowedUpTest nélkül elindulni, úgy, hogy már töröltem :) azután is, hogy a sample mappát töröltem :)
     * semmi értelme, nem is kell hogy legyen, de így fut :) :) :) :) :) :) :) :) nem értem a hibát :).
     */
    @Test
    void explode() {
        int a=1;
        assertEquals(1,a);
    }
}